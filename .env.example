# Configuração do banco de dados MySQL no Railway
# Copie este arquivo para .env e preencha com os dados do seu projeto Railway

# MySQL Connection (Railway fornece estes dados automaticamente)
DB_HOST=containers-us-west-xxx.railway.app
DB_PORT=6379
DB_USER=root
DB_PASSWORD=sua_senha_aqui
DB_NAME=railway

# Ou use a variável MYSQL_URL fornecida pelo Railway
MYSQL_URL=mysql://root:password@host:port/railway

# Porta do servidor
PORT=3000
